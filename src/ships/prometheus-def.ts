import type { ShipDef } from "@/ships/defs";
import { F11_HALBERD } from "@/ships/defs";

/** PLACEHOLDER: the BC-303-style carrier hull is being authored; until then it is the fighter scaled up. */
export const PROMETHEUS: ShipDef = { ...F11_HALBERD, name: "Prometheus (placeholder)" };
